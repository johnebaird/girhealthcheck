apiVersion: v1
kind: Service
metadata:
  name: {{ include "girhealthcheck.fullname" . }}
  labels:
    {{- include "girhealthcheck.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "girhealthcheck.selectorLabels" . | nindent 4 }}
